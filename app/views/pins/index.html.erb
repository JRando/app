<%= render 'pages/home' unless user_signed_in? %>

    <div id="pins" class="transitions-enabled">
      <% @pins.each do |pin| %>
        <div class="box panel panel-default">
          <%= link_to image_tag(pin.image.url(:medium)), pin %>
         
      <% if current_user && pin.user == current_user %>
            <div class='actions', style= "margin: 4px">
              <%= link_to edit_pin_path(pin) do %>
                <span class="glyphicon glyphicon-pencil"></span>
              <% end %>
              <%= link_to pin, method: :delete, data: { confirm: 'Forreal?' } do %>
                <span class="glyphicon glyphicon-remove"></span>
              <% end %>
            </div>
           <% end %>

          <div class='panel_body', style= "margin: 4px">
          By: <strong><%= pin.user.name if pin.user %></strong></br>
          <%= pin.description %>
            



         </div>
         </div>
  <% end %>
</div>

<center><%= will_paginate @posts, renderer: BootstrapPagination::Rails %></center>

</div>

